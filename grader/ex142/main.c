# เช็คเลขคู่แบบคนไม่ปกติ

พี่ภาคอยากเขียนโค้ดเช็คตัวเลขจากตัวแปร int number เป็นเลขคู่รึเปล่า แต่ไม่อยากเช็คแบบคนปกติ อยากเช็คแบบ*ไม่มี*ประสิทธิภาพเลย โดยเอาตัวเลขเนี่ยมาเช็ค if-else เรื่อยๆ จนกว่าจะเจอ แต่ถ้าเขียน if-else เยอะขนาดนี้ด้วยมือก็เหนื่อย จะใช้งาน ChatGPT ก็สงสารมัน น้องช่วยพี่เขียนโค้ดเพื่อสร้าง if-else ทั้งหมดหน่อยสิ เราจะเริ่มเช็คตั้งแต่เลข 0 จนถึงเลขที่พี่จะใส่เข้าไป พร้อมไหม!

Input

จำนวนเต็ม 1 จำนวน ซึ่งจะเป็นเลขเต็มบวกเท่านั้น ถ้าเป็นชนิดอื่นให้แสดงผลออกมาว่า "error!"

Output

function isEven โดยใช้ if-else ที่เช็คตัวเลขจากตัวแปร number ว่าเลขนั้นเป็นเลขคู่รึเปล่า อาจจะยังงงๆ แนะนำให้ดูจาก testcase เลยดีกว่า

![literally yanderedev](https://ceboostup-api.natthapas.dev/attachments/dbccabd9-ee1d-4818-948d-f83b01544f5a/FgvngvyXgAEMwvy.jpg)

## Solution

```c
// Your code here
```