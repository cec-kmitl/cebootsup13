# Pangram

พี่อยากรู้ว่าประโยคที่พี่อ่านอยู่นั้นเป็น Pangram หรือเปล่า แต่อยากให้ช่วยเขียนโปรแกรมตรวจเช็คให้หน่อย

## 🔤 Pangram คือ?
Pangram คือประโยคที่ใช้ตัวอักษรอังกฤษทั้งหมด 26 ตัว (A-Z)

## 🗨️ คำอธิบาย Input-Output
Input เป็น `str` 1 ประโยค **อาจมีตัวอักษรพิมพ์เล็กและพิมพ์ใหญ่** (ในที่นี้จะเว้นวรรคด้วย Underscore `_` แทน)
Output เป็น `Pangram` หรือ `Not a pangram`

## Solution

```c
// Your code here
```