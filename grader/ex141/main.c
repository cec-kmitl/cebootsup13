# No fun allowed

**ทำข้อ 139. อับดุลเอ๋ย กับ 140. No likes allowed ก่อนนะ**

ไม่ใช่แค่ทีมของ Elongated Muskrat ที่ยึดการทำงานทั้งหมดของ Twitter ทีมของ Marker Elliot Zuccing Professional the 3rd ได้เดินทางมาจาก galaxy อื่นมายึดการทำงานของ Facebook เช่นกัน ตอนนี้โลกอยู่ในสถานะที่ไม่มีใครติดต่อสื่อสารกันได้ ทหารของเราต้องการเปลี่ยนคำหลายคำเลย ดังนั้น น้องๆต้องช่วยทหารของเราเขียนโค้ดในการแทนที่ข้อความ

**ใช้ได้แค่ header stdio.h และ iostream -- ใช้ library string ไม่ได้** 

Input

เป็นข้อความ 3 บรรทัด
บรรทัดที่ 1 จะใส่ข้อความต้นฉบับของเรา
บรรทัดที่ 2 จะใส่ข้อความที่จะต้องการเลี่ยน (pattern)
บรรทัดที่ 3 จะใส่ข้อความที่จะเปลี่ยนเป็น (replacement)
(ถ้างงให้ดู testcase)

Output

ข้อความที่ได้แทนที่แล้ว โดยจะหาข้อความที่มีในบรรทัดที่ 1 ทั้งหมด และแทนที่โดยบรรทัดที่ 2



![zucc rizz](https://ceboostup-api.natthapas.dev/attachments/7f0010b3-a423-46ed-b46e-a926e30d0b44/zucc_rizz.avif)
![zucc](https://ceboostup-api.natthapas.dev/attachments/ead25551-ca09-4cd8-ba18-6c90952f2b8c/zucc.avif)

## Solution

```c
// Your code here
```