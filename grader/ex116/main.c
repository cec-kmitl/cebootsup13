# Introduction to C-Lowlevel 

เขียนโปรแกรม C โดย **ไม่ใช้ `stdio.h`** เลย  
ใช้แค่ `read()` กับ `write()` จาก `<unistd.h>` เท่านั้น!

เขียนโปรแกรมที่:
- รับ input เป็นเลขจำนวนเต็ม 2 ตัว คั่นด้วยช่องว่าง (เช่น `12 30`)
- แสดงผลลัพธ์เป็น `42`
- อะไรนอกเหนือจากที่กล่าวมา `error` ทุกกรณี

**อยากทำข้อนี้ได้ ถามพี่เชฟวี่นะ~**

## Solution

```c
// Your code here
```