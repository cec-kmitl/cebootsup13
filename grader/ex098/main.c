# 🐉 ตำนานแห่งบันไดเทพเจ้า

ในยุคสมัยที่เวทมนตร์และตำนานยังคงผสมผสานอยู่กับความจริง ดินแดนแห่งหุบเขาอสูรสลายได้กลายเป็นสถานที่เลื่องชื่อในเรื่องความลึกลับและอำนาจอันไม่อาจหยั่งถึง ที่นั่นมีบันไดศักดิ์สิทธิ์ซึ่งถูกกล่าวขานว่าถูกสร้างขึ้นโดยเทพเจ้าผู้โบราณ บันไดนี้ทอดยาวขึ้นสู่ฟากฟ้ามีประมาณ `50` ชั้น ใครก็ตามที่สามารถปีนขึ้นไปถึงขั้นใดขั้นหนึ่งได้ จะได้รับพลังเวทอันยิ่งใหญ่และความรู้ที่ลึกซึ้งเหนือกว่ามนุษย์ทั่วไป

แต่การปีนบันไดนี้ไม่ได้ง่ายดาย เพราะแต่ละขั้นบันไดไม่ได้เป็นเพียงแค่บันไดธรรมดา แต่ละขั้นนั้นถูกคำนวณด้วยสูตรลับที่ผสมผสานระหว่างลำดับเลข Fibonacci อันศักดิ์สิทธิ์ กับการเลื่อนบิตทางซ้าย (bitwise left shift) ซึ่งเป็นเทคนิคเวทมนตร์โบราณที่ช่วยเพิ่มพลังให้กับแต่ละขั้นอย่างมหาศาล นักเวทและนักผจญภัยต่างพากันมุ่งหวังที่จะเข้าใจและไขความลับของสูตรนี้ เพื่อเตรียมพร้อมสำหรับการปีนบันไดและรับพลังแห่งเทพเจ้า

ภารกิจของคุณในครั้งนี้คือการเขียนโปรแกรมช่วยนักเวทผู้กล้าที่สามารถคำนวณค่าพลังเวทของขั้นบันไดที่ `n` ได้อย่างแม่นยำและมีประสิทธิภาพ แม้ว่าการคำนวณจะซับซ้อนและไม่มีที่สิ้นสุดก็ตาม คุณจะต้องใช้พลังแห่งการเรียกซ้ำ (recursion) เพื่อพิชิตข้อจำกัดที่ห้ามใช้คำสั่งวนลูป และป้องกันไม่ให้ระบบล่มจากการเรียกซ้ำลึกเกินไป ด้วยความรู้ความสามารถนี้ คุณจะเป็นผู้กล้าที่สามารถไขความลับของบันไดเทพเจ้าและก้าวสู่ระดับใหม่ของพลังอันมหาศาลได้

เตรียมพร้อมแล้วหรือยังที่จะออกเดินทางไปกับบันไดเทพเจ้าสะท้านฟ้า? จุดเริ่มต้นแห่งการผจญภัยครั้งยิ่งใหญ่อยู่ในมือคุณแล้ว!

---

## 🧙‍♂️ เคล็ดลับจากนักเวทย์ที่ท่านเจอระหว่างที่กำลังท้อ

- แต่ละขั้นที่ `n` ของบันไดมีค่าคำนวณจากผลรวมของ Fibonacci แต่ละตำแหน่ง ที่ถูกย้ายบิตซ้ายด้วยจำนวนเท่ากับตำแหน่งนั้น เช่น:
  - ขั้นที่ 3 = Fib(1) × 2^1 + Fib(2) × 2^2 + Fib(3) × 2^3
  - โดยที่ Fib(1) = 1, Fib(2) = 1, Fib(3) = 2
  - **ป้องกัน stack overflow** จากการเรียกฟังก์ชันซ้ำลึก ๆ เช่น ใช้ memoization หรือจำกัดช่วงค่าของ `n`
  - การใช้ bitwise left shift สามารถใช้ operator << แทนการใช้ × 2^n ได้เลยเช่น `Fib(n) << n == Fib(n) × 2^n`
  - ถ้ารันแล้ว Compiler error ลองรันใหม่

---

## Solution

```c
// Your code here
```