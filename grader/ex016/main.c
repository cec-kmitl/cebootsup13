# หนึ่งล้วนๆ

## หนึ่งล้วนๆ
___

> ให้เขียนโปรแกรมเพื่อรับข้อมูลเป็นสตริงที่ประกอบด้วยตัวเลขฐานสอง (0 และ 1) จากนั้นให้พิจารณาเฉพาะกลุ่มของเลข 1 ที่อยู่ติดกัน (เช่น 11, 111, 1 เป็นต้น) โดยให้ถือว่ากลุ่ม 1 เหล่านี้เป็นตัวเลขฐานสองคนละกลุ่ม และนำค่าที่ได้จากการแปลงเลขฐานสองแต่ละกลุ่มไปเป็นเลขฐานสิบ แล้วนำมาบวกกันทั้งหมด

## Solution

```c
// Your code here
```