# ฐานสองมันน้อยไป

น้องๆหลายๆคนคงเคยเรียนเรื่องการเปลี่ยนเลขฐานต่างๆ เป็นเลขฐาน 10 แล้ว เช่น เลขฐานสอง 101 คือ 1*(2^2) +0*(2^1) +1*(2^0) = 5 แล้วถ้าเป็นเลขฐาน n  จะสามารถเขียนออกมาได้เป็น (0 ถึง n-1)*(n^(หลัก-1)) เพราะงั้น น้องลองเขียน โปรแกรมที่เปลี่ยนเลขฐานต่างๆตั้งแต่ฐาน 2 ถึง 10 เป็นเลขฐาน 10 ดู

## Solution

```c
// Your code here
```