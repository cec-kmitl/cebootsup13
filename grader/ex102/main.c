# ภารกิจมังกรไฟ 🐉

---

ณ ดินแดนอัลเทเรียอันไกลโพ้น มังกรไฟตนหนึ่งนามว่า **“ไรซิน”** ได้ทำการทดสอบเหล่านักรบที่กล้าท้าทายมัน  
โดยการให้แต่ละคนป้อนตัวเลขจำนวนหนึ่งเข้าไปในถ้ำของมัน หากผลรวมของตัวเลขเหล่านั้นเป็นจำนวนที่ **หารด้วย 5 ลงตัว**  
มันจะยอมรับว่า _"เจ้าคือผู้กล้า!"_

> จงเขียนโปรแกรมที่รับตัวเลขจำนวน `n` ตัวจากผู้ใช้  
> และตรวจสอบว่า ผลรวมของตัวเลขทั้งหมดนั้น **หารด้วย 5 ลงตัว** หรือไม่  
>  
> - หากใช่ ให้พิมพ์:  
>   ```
>   You are Hero!
>   ```  
> - หากไม่ใช่ ให้พิมพ์:  
>   ```
>   You're too ordinary! Go back and train again!
>   ```

---

## 📥 Input:
- บรรทัดแรก: จำนวนเต็มบวก `n` (จำนวนตัวเลขที่จะป้อน)
- บรรทัดที่สอง: ตัวเลขจำนวน `n` คั่นด้วยช่องว่าง

## 📤 Output:
- ข้อความตามเงื่อนไขด้านบน

---


## Solution

```c
// Your code here
```