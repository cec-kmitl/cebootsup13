# notation to coordinates

พี่อยากเขียนโปรแกรมสำหรับ text-based chess แต่เพื่อให้ง่ายในการเขียนโปรแกรมเราต้องทำการ แปลงตารางพิกัดในตารางหมากรุกเป็นพิกัดตัวเลข

[chess notation](https://en.wikipedia.org/wiki/Algebraic_notation_(chess))

## input
รับค่าเป็น chess notation 

## output 
แสดงค่าในพิกัดแกน x (0 - 7) และแกน y (0 - 7) หรือถ้า input ไม่ถูกต้องให้แสดง Invalid input

> **example**  e5 -> e = 4, 5 = 4 ดังนั้น output จะเป็น 4 4

## Solution

```c
// Your code here
```